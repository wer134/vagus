# 시각화 계획 (VISUALIZATION PLAN) — autonomous-network-mgmt

> 작성일: 2026-09-13
> 선행 문서: `ROADMAP.md`(고도화 계획, 특히 D-4·E-3·E-4), `AUDIT_2026-09-09.md`(감사), `PROJECT_OVERVIEW.md`
> 대상: 이 프로젝트의 결과를 **사람이 읽는 그림**으로 만드는 일 전체 — 문서 도판, 정적 대시보드,
> 향후 라이브 대시보드.

---

## 0. 현재 상태

### 0.1 가진 것

| 자산 | 현황 |
| --- | --- |
| `dashboard.html` (= 루트 `index.html`, 735 L) | Chart.js 차트 7개. **네트워크 호출 없음** — 메트릭은 JS가 자체 생성하고 실험 결과는 `EMBEDDED_SUMMARY` / `EMBEDDED_EFFICIENCY` 상수에 하드코딩 |
| `results/cicddos_validation_v2_curve.png` | rolling F1 학습곡선 + 베이스레이트 점선. `validate_security_detector.py` 안에서 matplotlib으로 생성 |
| `results/*.json` 22개 | 에피소드 단위 원자료까지 들어 있어 도판 재료는 충분 (§0.3) |

### 0.2 문제 — 대시보드가 철회된 주장을 그리고 있다

차트 7개가 전부 **현재 문서가 철회했거나 대체한 수치**를 렌더링한다. 이 페이지는 GitHub Pages로
공개되는 가장 눈에 띄는 산출물이고, README와 정면으로 어긋난다.

| 차트 | 그리는 것 | 문제 |
| --- | --- | --- |
| `chart-ttr`, `chart-box`, `chart-success` | `EMBEDDED_SUMMARY.fewshot` — TTR 3.78, 분포 2/3/4/5 | **사이클당 2틱**으로 측정된 값 (AUDIT P1). 1틱 기준 재측정은 6.86, 분포 6~11 |
| `chart-bar` | PPO 200.0 vs MAML 3.78 | 200.0은 **미학습 랜덤 정책**이고 측정 경로도 다르다 — 2026-09-06에 철회됨 |
| `chart-eff`, `chart-ppo-eff`, `chart-maml-eff` | `EMBEDDED_EFFICIENCY` 샘플 효율 곡선 | 근거가 된 "500배 샘플 효율" 문구는 **삭제됐다**. 곡선만 남아 주장을 대신하고 있다 |

페이지 상단에 "정적 데모" 배지와 재측정 안내 문구는 붙였지만(2026-09-09), **그림 자체는 그대로**다.
문구로 덧붙인 경고보다 그림이 먼저 읽힌다.

### 0.3 문제 — 지금 가장 중요한 발견들이 그림이 없다

감사·로드맵에서 나온 핵심 결론은 전부 표와 산문으로만 있다.

- 정책 붕괴 (MAML은 행동 1종, PPO는 6종) — `policy_check.json`에 분포가 있는데 시각화 없음
- 부수 피해 (combined가 TTR 이득 없이 정상 링크를 0.86회/ep 건드림) — 절제의 핵심인데 표뿐
- 우회의 대가 (B-1: cost 올리면 이웃 링크 부하 1.0 → 1.5) — 시뮬레이터가 바뀐 이유 자체
- 오프라인 성공률 50%가 "링크가 우연히 맞은 비율"이라는 것 — `ttr_list`가 2.5/200/4.0/200으로
  교대하는 게 그대로 보이는데 아무도 그리지 않았다

---

## 1. 원칙

`FIX_PLAN.md` §0.2와 `ROADMAP.md` §1을 그림에도 그대로 적용한다.

1. **모든 도판은 결과 JSON에서 스크립트로 생성한다.** 손으로 숫자를 넣은 그림은 손으로 고친
   결과 파일과 같은 종류의 결함이다. 대시보드의 `EMBEDDED_*` 상수가 바로 그 사례다.
2. **도판은 측정 조건을 달고 다닌다.** E-4가 결과 JSON에 넣은 `git_commit` · `seed` ·
   `contract`(obs/action/reward/sim 버전)를 캡션에 그대로 싣는다. 조건 없는 그림은 P1과 같은
   결함 — 나중에 어느 자로 잰 건지 알 수 없다.
3. **나쁜 결과를 그릴 수 있어야 한다.** 잘 나온 경우에만 말이 되는 차트는 틀린 차트다.
   `cicddos_validation_v2_curve.png`가 좋은 선례다 — "시간이 지나도 베이스레이트를 못 넘는다"를
   그림으로 증명한다.
4. **철회된 수치는 다시 그리지 않는다** (§5).

여기에 차트 자체의 규칙을 더한다 (`dataviz` 스킬):

5. **이중 축 금지.** 스케일이 다른 두 측정은 차트 두 개나 small multiples로. TTR과 부수 피해를
   한 축에 얹고 싶은 유혹이 정확히 이 경우다 (F2).
6. **색은 역할로 고른다.** 크기 비교 = 순차(한 색조), 정체성 구분 = 범주(고정 순서), 하나가
   주인공 = 강조(1색 + 회색). 범주 색은 순서대로 쓰고 순환시키지 않는다.
7. **팔레트는 눈으로 판정하지 않고 검증기를 돌린다** (`dataviz/scripts/validate_palette.js`).
8. 계열 2개 이상이면 범례는 항상, 4개 이하면 직접 라벨도 함께. 색만으로 정체성을 주지 않는다.

---

## 2. 도구와 구조

### 2.1 렌더러 두 개, 팔레트 하나

| 용도 | 렌더러 | 근거 |
| --- | --- | --- |
| 문서 도판 (README·cowork·논문) | **matplotlib** → SVG + PNG | 이미 의존성에 있고 기존 곡선을 만든 도구. 정적이면 충분 |
| 대시보드 | **Chart.js** (기존 유지) | 이미 들어가 있고 호버·필터가 필요 |

두 렌더러가 각자 색을 고르면 반드시 어긋난다. **`experiments/figures/palette.py` 하나를 단일
출처**로 두고, matplotlib은 직접 import, 대시보드는 이 파일에서 생성한 CSS 커스텀 프로퍼티
(`--series-1` …)를 읽는다. 값은 `dataviz/references/palette.md`의 검증된 기본 팔레트를 그대로
채택하고, 채택 시점에 검증기를 한 번 돌려 결과를 파일 주석에 남긴다.

### 2.2 배치

```
experiments/
├── figures/
│   ├── palette.py          # 검증된 팔레트 단일 출처 (+ 라이트/다크 슬롯)
│   ├── figspec.py          # 캡션·조건 스탬프·저장(SVG+PNG) 공통 헬퍼
│   └── out/                # 생성물 (커밋함 — README가 참조)
└── make_figures.py         # 결과 JSON → figures/out/*.svg|png 전부 생성
```

`make_figures.py`는 `reproduce.sh`의 마지막 단계로 편입한다. 결과가 갱신되면 그림도 같은 실행에서
갱신되므로 둘이 어긋날 수 없다.

### 2.3 라벨 언어 — 이미지 안은 짧은 영어, 설명은 마크다운 캡션

기존 `_plot_learning_curve()`는 한글 라벨을 쓰려고 OS 폰트(맑은 고딕/AppleGothic/나눔고딕)를
찾는다. Linux CI에는 셋 다 없어 두부 문자가 난다. **이미지 안에는 축·범례만 짧은 영어/숫자로
넣고, 해설은 마크다운 캡션에 한글로 쓴다.** 폰트 의존이 사라지고 그림 재사용도 쉬워진다.

---

## 3. 도판 카탈로그

각 도판은 `무엇을 읽게 할 것인가(job) → 형태(form) → 색 역할(color job)` 순으로 정했다.

### F1. TTR 분포와 물리 하한 — `ttr_distribution`

- **Job**: 분포, 그리고 "거의 하한에 붙어 있다"는 사실
- **Form**: 세로 막대(TTR 4~11 도수) + **하한 참조선** (cost 적용 후 SLA 회복까지 4틱,
  `metric_generator` 자가 테스트가 재는 값) — 계열 1개이므로 범례 없음
- **Color**: 순차 1색조 + 참조선은 회색 점선
- **Data**: `stress_50ep.json.results[].ttr` (현재 4:1 5:1 6:21 7:17 8:5 9:2 10:2 11:1)
- **왜**: 대시보드의 2/3/4/5 분포를 대체한다. "TTR 6.86"이 나쁜 게 아니라 **하한 근처**임을
  한 장으로 보여준다.

### F2. 절제 — TTR은 같고 부수 피해만 늘어난다 — `ablation_pair`

- **Job**: 세 모드 비교. 핵심은 "combined가 analytics_only 대비 얻는 게 없다"
- **Form**: **가로 막대 두 패널(small multiples)** — 좌: 평균 TTR, 우: 부수 피해/ep.
  범주 순서 고정(analytics_only / maml_only / combined). **이중 축 절대 금지** — 이 그림이
  로드맵에서 가장 유혹이 큰 자리다
- **Color**: 패널별 순차 1색조. 동률인 두 막대(7.08 / 6.90)는 직접 라벨로 수치 노출
- **Data**: `ablation_study.json.results[*].{avg_ttr, wasted_actions_per_ep}`
- **왜**: 이 프로젝트의 가장 중요한 결론(복구는 Analytics가 한다)이 그림 한 장이 된다.

### F3. 정책 붕괴 — `policy_actions`

- **Job**: "MAML은 한 가지 행동만 한다"를 즉시 보이게
- **Form**: 체크포인트별 **가로 누적 막대**(행동 선택의 부분-전체). MAML은 한 덩어리,
  PPO는 6조각 — 대비가 그림 자체로 말한다. 옆에 엔트로피 **스탯 타일**(0.00 bit vs 1.70 bit)
- **Color**: 범주 팔레트 고정 순서, 행동 6종이라 상한(8) 이내. 범례 + 4개 이하 직접 라벨
- **Data**: `policy_check.json.{ppo,maml}.on_policy_counts`
- **왜**: P8은 지금 표로만 있다. 이 그림은 숫자를 몰라도 읽힌다.

### F4. 성공 50%는 정책이 아니라 우연 — `offline_by_link`

- **Job**: 에피소드별 성패가 **링크에 따라 갈린다**는 것
- **Form**: 링크별로 나눈 **스트립/도트 플롯** (x=TTR 로그 또는 구간, 점=에피소드).
  r3-r4는 2~5에 모이고 r1-r4는 전부 200(타임아웃)에 쌓인다
- **Color**: 강조 형태 — 타임아웃 점만 status(경고), 나머지는 기본 1색조
- **Data**: `offline_eval.json.{baseline,fewshot}.ttr_list` + `eval_links`
  (MAML: 2.5 / 200 / 4.0 / 200 … 교대)
- **왜**: "성공률 50%"라는 숫자의 실체를 드러낸다. 표로는 절대 전달되지 않는다.

### F5. 우회의 대가 — `routing_detour`

- **Job**: cost를 올리기 전/후 링크별 부하 변화
- **Form**: **덤벨**(before → after, 링크 6개) — 전형적인 before/after per item
- **Color**: 1색조 2단계(before 연함 / after 진함), 화살표 방향으로 증감
- **Data**: `metric_generator.get_link_loads()`를 스크립트가 직접 호출 (r3-r4 → 100 적용 전후).
  결과 JSON이 아니라 시뮬레이터를 부르는 유일한 도판 — **재현 코드를 캡션에 명시**
- **왜**: B-1이 왜 필요했는지(우회가 공짜였다)를 설명하는 가장 빠른 방법.

### F6. 탐지기 rolling F1 vs 베이스레이트 — `cicddos_curve` *(기존 이식)*

- **Job**: 시간이 지나도 자명한 베이스라인을 못 넘는다
- **Form**: 선 + **베이스레이트 참조선**(점선) + cold-start 구간 음영. 현재 형태 유지
- **Color**: 1색조 + 회색 참조선 + 중립 음영
- **작업**: `validate_security_detector.py` 안의 플로팅을 `make_figures.py`로 옮기고
  한글 라벨 제거, 팔레트 통일. **결과 자체는 재측정 없이 그대로** (CSV 없음)

### F7. 탐지기 혼동 행렬 — `cicddos_confusion`

- **Job**: TN/FP/FN/TP 크기 비교
- **Form**: 2×2 **히트맵** + 셀 값 직접 표기
- **Color**: 순차 1색조 (격자 크기 비교의 표준)
- **Data**: `cicddos_validation_v2.json.metrics.confusion_matrix` = `[[1880,1933],[2234,2652]]`
- **왜**: "F1 0.56"이 "전부 공격 예측으로 무너진 것은 아니다"(BENIGN 3,813 중 1,880 정답)를
  같이 보여준다 — 불리한 수치를 맥락과 함께 내는 이 리포의 기조에 맞는다.

### F8. OODA 폐쇄 루프 구조도 — `ooda_loop` *(차트 아님)*

- **Form**: 인라인 **SVG 다이어그램**. Observe→Orient→Decide→Act→Evaluate와 각 단계의 실제
  구현 함수, 그리고 **어느 계층이 실제로 일하는지**(Analytics override 경로를 굵게)
- **왜**: `PROJECT_OVERVIEW.md`의 ASCII 다이어그램을 대체하고, 절제 결론과 구조를 잇는다
- 작성 시 `artifact-diagramming` 가이드를 따른다

### F8 이후 — 학습·테스트 도판 (2026-09-13 추가)

앞의 F1~F8은 **이미 내린 결론을 설명하는** 그림이다. 아래 T1~T4는 **아직 열려 있는 질문에
답하기 위한** 그림이라 우선순위가 더 높다.

현재 학습은 아무것도 남기지 않는다 — MAML은 `meta_loss`를 20 iter마다 stdout에 찍고 버리고,
PPO는 `verbose=1`로 SB3 표를 찍고 버린다. 콜백도 로그 파일도 없고, `*.meta.json`은 **최종
체크포인트 상태만** 기록한다. 그래서 "붕괴했다"는 알지만 "언제 붕괴했는가"는 답할 수 없다.

#### T1. 학습 중 붕괴 곡선 — `train_collapse`

- **Job**: 정책이 **언제** 탐색을 그만두는가. P8의 유일한 미해결 질문
- **Form**: 선 그래프 **패널 2개** — 위: 행동 엔트로피(bit), 아래: 최빈 행동 비율 +
  붕괴 임계선 0.8. 스케일이 다르므로 **이중 축 금지**. MAML/PPO 2계열이므로 범례 + 직접 라벨
- **Color**: 범주 슬롯 1·2 (blue/orange), 임계선은 회색 점선
- **Data**: 신규 — `train()`에 주기적 정책 프로브 콜백 → `results/train_curve_{maml,ppo}.json`
- **왜**: "붕괴했다"가 "iter ~N에서 붕괴하고 회복하지 못했다"가 되면, A-3(보상)이냐
  A-4(커리큘럼)이냐를 가르는 근거가 된다. 지금은 그 데이터를 매번 버리고 있다

> **손실 곡선은 그리지 않는다.** 실측 `meta_loss`는 +0.0069 / +0.0073 / +0.0276 / −0.0012 …
> −0.0141 / −0.0376 / −0.0066 / −0.0265 으로 **0 주변 진동이고 추세가 없다** — advantage를
> 정규화한 REINFORCE 손실이라 당연한 결과다. 그리면 과학적으로 보이지만 아무 의미가 없다.
> **손실이 아니라 행동을 그린다.**

#### T2. 체크포인트 이력 스코어보드 — `checkpoint_history`

- **Job**: Track A의 각 변경(A-1 관측 v2, A-3 보상 v2 …)이 실제로 붕괴를 풀었는가
- **Form**: 체크포인트를 시간순으로 놓은 **소형 다중** — 엔트로피 / 최빈 비율 / TEST 성공률.
  변경 지점(sim 버전·계약 버전 전환)을 축에 표시
- **Color**: 순차 1색조 + 붕괴 판정은 status(경고) 마커
- **Data**: **이미 존재** — `agents/*.meta.json`에 commit·seed·계약 버전·붕괴 통계가 있다.
  모으기만 하면 된다
- **왜**: Track A의 진척을 한 장으로. 지금은 재학습할 때마다 결과가 대화 속으로 사라진다

#### T3. seed 분산 — `seed_spread` *(A-7 이후)*

- **Job**: 결과를 seed 하나가 끌고 가는지
- **Form**: seed별 점을 **전부** 찍는 도트 플롯 + 평균선. 평균±표준편차 막대만 그리지 않는다
- **왜**: A-7이 seed 5개를 요구한다. 평균만 그리면 "단일 seed 수치 금지" 규칙의 정신을 배신한다

#### T4. 주장–검증 매핑 — `claim_coverage` *(테스트 쪽 유일한 도판)*

- **Job**: `ROADMAP.md` §0의 주장 각각을 **무엇이 자동으로 검증하는가**
- **Form**: 주장 × 근거유형(자가 테스트 / 결과 파일 / 없음) **매트릭스**. 대부분 "없음"으로
  나올 것이고 **그 공백이 정보다**
- **왜**: §0 표는 지금 수작업으로 유지된다. 장식이 아니라 정직성 인프라

#### 테스트 시각화에 대한 판단 — 대체로 하지 않는다

- 자가 테스트 9개는 전부 pass/fail이다. 항목이 7개를 넘고 각각 의미를 가지므로 **표가 맞는
  형태**이고, `selftest.sh`의 터미널 출력이 이미 그 표다. 차트로 만들어도 정보가 늘지 않는다
- 의미 있는 건 **추세**(언제부터 깨졌나·소요 시간·플레이키)인데 **CI(E-2)가 없어 이력 자체가
  없다.** 그릴 데이터가 없다 — E-2가 들어가고 실행이 쌓인 뒤에 다시 볼 문제
- 당장은 차트보다 **CI 배지** 하나가 실용적이다
- TensorBoard 류 학습 대시보드는 과하다 — 학습이 5분이고 체크포인트가 3개인 규모다

### 우선순위

**원칙: 설명된 결론보다 열린 질문에 답하는 도판이 먼저다.** (2026-09-13 재작성)

| 순위 | 도판 | 근거 |
| --- | --- | --- |
| 1 | **T1** 학습 중 붕괴 곡선 | 유일한 미해결 질문(P8)에 답한다. 지금 데이터를 버리고 있어 콜백이 먼저 |
| 2 | **T2** 체크포인트 이력 | 데이터가 이미 있다. Track A의 스코어보드가 된다 |
| 3 | F2 절제 쌍, F3 정책 붕괴 | 현재 가장 중요한 두 결론에 그림이 없다 |
| 4 | F1 TTR 분포, F4 링크별 성패 | 대시보드의 잘못된 분포·비교를 대체할 것들 |
| 5 | F5 우회의 대가, F8 구조도 | 메커니즘 설명 (왜 이렇게 바꿨는가) |
| 6 | F6 CICDDoS 곡선 이식, F7 혼동 행렬 | 기존 자산 이식 + 보완 |
| 7 | T3 seed 분산, T4 주장–검증 매핑 | 각각 A-7 / E-2 이후에 의미가 생긴다 |

---

## 4. 단계

### V-Phase 1 — 도판 파이프라인 (F1~F5, F7)

1. `figures/palette.py` + 검증기 1회 실행, 결과를 주석으로 기록
2. `figures/figspec.py` — 캡션에 `git_commit`/`seed`/`contract` 자동 스탬프, SVG+PNG 동시 저장
3. **T1 학습 프로브 콜백** — MAML `train()`과 PPO(SB3 `BaseCallback`)에 주기적
   `policy_action_stats` 호출을 붙여 `results/train_curve_{maml,ppo}.json` 생성
4. `make_figures.py`에 **T1 · T2 · F2 · F3**(1차) → F1 · F4 · F5 · F7(2차) 구현
5. `reproduce.sh` 마지막 단계로 편입, `selftest.sh`에 "그림이 최신 결과 커밋과 같은가" 검사 추가
6. README·`ROADMAP.md` §6·`AUDIT_2026-09-09.md` §3에 도판 삽입

**완료 기준**: `./experiments/reproduce.sh` 한 번으로 결과 JSON과 도판이 함께 갱신되고,
모든 도판 캡션에 측정 조건이 박혀 있다.

### V-Phase 2 — 대시보드 정리 (ROADMAP D-4 선행)

**이 단계의 목표는 "예쁘게"가 아니라 "틀린 그림을 내리는 것"이다.**

1. `EMBEDDED_SUMMARY` / `EMBEDDED_EFFICIENCY`를 **결과 JSON 임포트**로 교체
   (빌드 시 `results/*.json` → 단일 `dashboard_data.json` 생성)
2. 샘플 효율 차트 3종(`chart-eff`, `chart-ppo-eff`, `chart-maml-eff`) **제거** — 근거 문구가
   이미 삭제된 주장이다 (§5)
3. `chart-bar`의 랜덤 정책 베이스라인 제거, F2(절제 쌍) + F3(정책 붕괴)로 교체
4. `chart-ttr`/`chart-box`/`chart-success`를 F1/F4 기준으로 재작성
5. 팔레트를 `palette.py` 생성 CSS 변수로 통일, 다크 모드 스텝 별도 선택, 호버 툴팁 추가
6. 루트 `index.html` 동기화 유지 (바이트 동일 — GitHub Pages 요구)

**완료 기준**: 대시보드의 모든 수치가 `results/`의 현재 값과 일치하고, 어느 차트도 철회된
주장을 그리지 않는다.

> **2026-09-13 완료** — 상세는 §8. 1·2·3·6은 계획대로, 4는 F1(분포)만 반영하고 F4(링크별
> 성패)는 문서 도판으로 남겼다. 5의 팔레트 통일은 `window.ANM_DATA.palette`로 처리했다.

### V-Phase 3 — 라이브화와 다이어그램 (ROADMAP D-3/D-4 이후)

- F8 구조도 + 토폴로지/라우팅 다이어그램
- `/history` API(D-3)가 생기면 대시보드가 실시간 OODA 사이클을 그림 — 정적 빌드는 Pages용으로 유지

---

## 5. 시각화하지 않을 것

**그리지 않는 것도 결정이다.** 그림은 주장을 되살린다.

| 대상 | 이유 |
| --- | --- |
| `sample_efficiency.json` 곡선 | 근거가 된 "500배 샘플 효율" 문구가 2026-09-06에 삭제됐다. 데이터는 남기되 **그리지 않는다** |
| `summary.json`의 baseline (200.0) | 미학습 랜덤 정책, 30 에피소드, 다른 측정 경로. 비교 차트에 올리는 순간 철회된 주장이 부활한다 |
| sim_v1/v2/v3 TTR을 한 축에 얹은 추이 차트 | **서로 다른 자로 잰 값**이다 (2틱 vs 1틱 vs 라우팅 변경). 한 축에 놓으면 "개선/악화"로 읽힌다. 전/후 병기는 **표**로만 하고, 굳이 그린다면 변한 것은 자이므로 "사이클당 틱 수"를 그린다 |
| 폐쇄 루프 TTR vs 오프라인 TTR 비교 | 측정 경로가 다르다. AUDIT P1이 잡은 실수를 그림으로 반복하는 꼴 |
| 정책 붕괴 체크포인트의 "성공률" 단독 막대 | F3(행동 분포)와 **반드시 같이** 낸다. 성공률만 떼면 상수 정책이 유능해 보인다 |

---

## 6. 리스크

| 리스크 | 대응 |
| --- | --- |
| 대시보드가 공개 페이지라 잘못된 그림의 노출이 가장 크다 | V-Phase 2를 F6/F7보다 앞에 둔다. 급하면 문제 차트 3종을 먼저 내리는 것만으로도 개선 |
| 렌더러 두 개(matplotlib/Chart.js)의 색·수치 드리프트 | 팔레트 단일 출처 + 대시보드가 결과 JSON을 직접 읽게 (임베드 금지) |
| 도판이 결과보다 오래돼 어긋남 | `make_figures.py`를 `reproduce.sh`에 편입 + 도판 캡션의 커밋과 결과 JSON 커밋 일치 검사 |
| 한글 폰트 부재로 두부 문자 | 이미지 안 라벨은 영어/숫자, 해설은 마크다운 캡션 (§2.3) |
| 커밋되는 바이너리 증가 | SVG를 1차 산출물로(텍스트, diff 가능), PNG는 README 호환용으로만 |
| "예쁘게 만들다 주장이 세지는" 전형적 실패 | §5 목록과 원칙 3(나쁜 결과를 그릴 수 있어야 한다)을 리뷰 체크리스트로 사용 |

---

## 7. 당장 시작할 수 있는 것

1. `figures/palette.py` + 검증기 1회 실행 — 나머지 전부의 전제
2. **T2 체크포인트 이력** — `agents/*.meta.json`만 모으면 된다. 의존성 없음
3. **T1 학습 프로브 콜백** — 콜백 하나. 이후 재학습부터 곡선이 쌓인다
4. **F3 정책 붕괴** — `policy_check.json`만 있으면 그려진다
5. **F2 절제 쌍** — `ablation_study.json`만 필요
6. **대시보드 샘플 효율 차트 3종 제거** — 삭제된 주장을 그리고 있다. 코드 삭제뿐

1~5면 "열린 질문에는 그림이 없고 닫힌 결론만 표로 있는" 현재 상태가 뒤집히고,
6이면 공개 페이지가 틀린 그림을 보여주는 상태가 끝난다.

---

## 8. 진행 기록

### 2026-09-13 — 인프라 + T1·T2·F2·F3

| ID | 산출물 | 상태 |
| --- | --- | --- |
| 인프라 | `experiments/figures/palette.py` | 완료 — 검증기 출력을 파일에 기록, matplotlib·Chart.js 공용 |
| 인프라 | `experiments/figures/figspec.py` | 완료 — **조건 없는 도판은 저장이 거부된다**, 이미지 안 한글 라벨도 거부 |
| 인프라 | `experiments/make_figures.py` | 완료 — `reproduce.sh` 마지막 단계, 자가 테스트는 `selftest.sh` |
| **T1** | `out/T1_train_collapse.*` | 완료 — 학습 프로브 신규 구현 (ROADMAP §5 참고) |
| **T2** | `out/T2_checkpoint_history.*` | 완료 — 기존 `*.meta.json`만으로 |
| **F2** | `out/F2_ablation_pair.*` | 완료 |
| **F3** | `out/F3_policy_actions.*` | 완료 |

**T1이 답한 것**: MAML은 학습의 24% 지점에서, PPO는 16% 지점에서 붕괴한다. PPO는 회복하고
MAML은 갇힌다 — 상세는 `ROADMAP.md` §5.

**부수 성과**: T1의 비침습성을 검증하다 학습 재현성 결함을 발견·수정했다 (`ROADMAP.md` §6).
`policy_check.json`이 E-4 메타데이터 규약에서 빠져 있던 것도 함께 고쳤다.

### 작업 중 배운 것

- **실행 중인 bash 스크립트를 수정하면 안 된다.** `reproduce.sh`를 돌리면서 편집했더니 bash가
  바이트 오프셋으로 이어 읽다 엉뚱한 위치에서 재개해 구문 오류로 죽었다. 파이프라인 수정은
  실행이 끝난 뒤에.
- **모듈 싱글턴 시뮬레이터를 관찰자가 빌려 쓰려면 스냅샷이 필요하다.** `metric_generator`는
  모듈 전역이라 프로브가 `reset_state()`를 부르면 학습 환경이 날아간다.

### 2026-09-13 — V-Phase 2 대시보드 정리

공개 페이지가 철회된 수치를 그리던 상태가 끝났다. 차트 7개 전부를 내리고 다시 짰다.

| 전 | 후 |
| --- | --- |
| `EMBEDDED_SUMMARY`·`EMBEDDED_EFFICIENCY` (2026-04-22 하드코딩, TTR 3.78) | `dashboard_data.js` — `make_dashboard_data.py`가 `results/*.json`에서 생성 |
| `chart-eff`·`chart-ppo-eff`·`chart-maml-eff` + 샘플 효율 표 | **삭제** (근거 문구가 이미 철회된 주장, §5) |
| `chart-bar` — PPO(미학습, 랜덤) vs MAML 막대 | 절제 실험 2분할: 평균 TTR / 부수 조치 (F2, 이중축 금지) |
| `chart-ttr` — 에피소드 번호별 TTR 꺾은선 | 복구 시간 **분포** 히스토그램, 학습 링크 vs 미학습 링크 (F1) |
| `chart-box` — 5개 분위수를 막대로 그린 "박스 플롯" | 학습 중 행동 엔트로피 / 최빈 행동 점유율 곡선 (T1) |
| `chart-success` — 두 쌍의 성공·실패를 한 도넛에 4조각 | 오프라인 평가 해결/미해결 누적 막대 + 체크포인트 행동 분포 (F3) |
| 탭 3 "샘플 효율" | 탭 3 "학습 동역학" — T1 곡선 + T2 체크포인트 표 |

**수치를 손으로 넣지 않는다**가 이 단계의 핵심이다. 대시보드는 `fetch()`를 못 쓴다
(`file://`는 CORS, Pages에는 백엔드가 없다). 그래서 값을 페이지 안에 넣어야 하는데, 그것을
사람이 타이핑하면 §1-1이 금지하는 바로 그 결함이 된다. `make_dashboard_data.py`가
`window.ANM_DATA`를 생성하고 `reproduce.sh`가 도판 직후에 그것을 부른다 — 결과·도판·대시보드가
같은 실행에서 갱신되므로 셋이 어긋날 수 없다.

각 탭 머리에 도판과 같은 **조건 스탬프**(커밋·계약 버전·seed·측정 조건)를 붙였다.
그림에는 요구하면서 대시보드에는 요구하지 않을 이유가 없다.

**검사**: `scripts/dashboard_smoke.js` — node로 DOM/Chart.js를 흉내 내 ① 모든 차트가 실제
데이터로 예외 없이 그려지는지 ② 이중축이 없는지 ③ 다계열인데 범례를 끈 차트가 없는지
④ 화면의 KPI가 결과 파일의 값과 같은지 ⑤ 데이터 파일이 없어도 죽지 않는지를 본다.
`selftest.sh`가 node가 있을 때 함께 돌린다.

### 작업 중 배운 것 (2)

- **그림 실패가 표까지 지우면 안 된다.** 헤드리스 브라우저로 렌더해 보니 Chart.js CDN이
  막힌 환경에서 첫 `new Chart()`의 예외가 렌더 함수를 통째로 중단시켜 **표와 KPI까지** 비었다.
  그림은 표의 보조지 대체물이 아니므로, 표·수치를 먼저 그리고 차트는 부재를 견디게 고쳤다.
  스모크 테스트에 이 경로를 넣었다.
- **브라우저로 실제 렌더해 보기 전에는 "된다"고 말할 수 없다.** 노드 스텁 테스트는 전부
  통과했지만 화면은 비어 있었다. 로직 테스트와 렌더 확인은 다른 것을 본다.

**남은 한계**: 이 환경에서는 CDN이 막혀 Chart.js를 실제로 불러온 화면을 보지 못했다.
차트 **설정**은 스모크 테스트로 검증했고 표·KPI·조건 스탬프·대체 경로는 스크린샷으로
확인했지만, 그려진 그림 자체는 미확인이다.

### 다음

1. F1 TTR 분포, F4 링크별 성패 — 문서 도판으로도 (대시보드에는 들어갔다)
2. F5 우회의 대가, F8 구조도, F6·F7 이식
3. T3(seed 분산)·T4(주장–검증 매핑)는 각각 A-7·E-2 이후
4. V-Phase 3 라이브화는 ROADMAP D-3(`/history` API) 이후
