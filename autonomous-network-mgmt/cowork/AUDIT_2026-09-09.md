# 코드 감사 보고서 및 수정 계획 (2026-09-09)

> **선행 문서**: `cowork/PROJECT_OVERVIEW.md` (구조 설명), `cowork/FIX_PLAN.md` (2026-09-06 지시서, T1–T8 완료)
> **범위**: `autonomous-network-mgmt/` 전체 소스 (Python 3,611 L, Java 593 L)를 직접 읽고,
> 핵심 의심 지점은 HTTP 없이 시뮬레이터를 직접 구동해 재현했다.
> **원칙**: `FIX_PLAN.md` §0.2를 그대로 따른다 — 지표를 좋게 보이도록 조정하지 않고,
> 결과 JSON을 손으로 고치지 않으며, 기존 한계 서술을 지우지 않고, 공개 시그니처를 깨지 않는다.

이 문서는 세 부분이다. §1 발견된 문제(근거·재현), §2 수정 계획(태스크·검증·완료 기준),
§3 수행 결과(전/후 수치 — 작업 진행에 따라 갱신).

---

## 1. 발견된 문제

`PROJECT_OVERVIEW.md` §7에 이미 기록된 한계(시뮬레이션 한계, CICDDoS 피처 문제, 미검증 영역 등)는
제외하고 **새로 확인된 것**만 적는다. 경로는 `autonomous-network-mgmt/` 기준.

### 1.1 실험 결과의 신뢰성에 직접 영향하는 문제

#### P1. 시뮬레이션 시간이 "관측 호출 횟수"로 흐른다 → TTR이 측정 스크립트에 의존

- **근거**: `simulation/metric_generator.py:124,129` — `get_node_metrics()`/`get_all_metrics()`가
  호출될 때마다 `_update_link_stress()`로 스트레스를 1스텝 진행시킨다. 벽시계 시간과 무관하며
  실험 스크립트의 `time.sleep(0.6)`은 물리적으로 아무 효과가 없다.
- `experiments/stress_test.py:99`, `ablation_study.py:40`, `persistent_buffer_test.py:81`이
  `/auto-step`(내부에서 `/metrics` 1회) 뒤에 **검증용 `/metrics`를 한 번 더** 호출한다 →
  OODA 1사이클당 시뮬레이터 2틱 진행.
- **재현** (`scratch/rca_trace.py`, HTTP 없이 `metric_generator` + `diagnose()` 직접 구동,
  analytics-only, 50 ep):

  | 사이클당 틱 | 평균 TTR |
  | --- | --- |
  | 2 (현재 측정 방식) | **3.94** — 보고치 `ablation_study.json` analytics_only 3.88과 일치 |
  | 1 (`/auto-step`만) | **6.68** |

- **의미**: 보고된 TTR 3.78~3.88의 절반 가까이는 측정 스크립트 자신이 회복을 가속한 결과다.
  대시보드나 collector(5초 폴링) 같은 관측자가 하나 더 붙으면 수치가 또 달라진다.
  `/metrics/<node>` 단일 노드 조회도 전체 링크를 진행시킨다.

#### P2. "RCA 정확도 100%"는 첫 사이클만 측정 — 2번째 사이클부터 정상 링크를 망가뜨림

- **근거**: `stress_test.py:130` `root_match = (first_root == link)`. 첫 판정만 본다.
- 2번째 사이클부터 정답 링크는 cost 100이 되어 `unhandled_links`에서 빠지고
  (`anomaly_detector.py:61`), `api_server._root_cause_analysis()`(`:240-265`)는 남은 인접 링크 중
  `(-shared, cost)` 최소 = **알파벳순 첫 링크**를 근본 원인으로 지목한다.
- **재현** (같은 스크립트): analytics-only 50 ep에서 **정상 링크 cost를 100으로 올린 횟수 112회**
  (에피소드당 ~2.2회). 예: `link=r1-r3 → actions=['r1-r3@100', 'r1-r2@100', 'r1-r4@100', 'r2-r3@100']`
- **의미**: TTR만 재고 "불필요한 라우팅 변경 횟수"라는 비용은 어디에도 기록되지 않는다.
  실망에서는 매 사이클 OSPF 재수렴을 유발하는 행동이다. combined 모드에서는 override 조건
  (`nodes_sharing_root >= 2`)이 2사이클째부터 안 맞아 MAML 행동이 그대로 나가는데,
  `persistent_buffer.json`이 기록한 2차 행동 `r1-r2@200`도 같은 종류의 부수 피해다.

#### P3. 폐쇄 루프 Orient에 IsolationForest가 실제로 쓰이지 않음 (문서-코드 불일치)

- **근거**: `api_server.py:343` — `/auto-step`은 `detector.update()`만 호출하고
  `detector.is_anomaly()`는 호출하지 않는다. 판정은 `diagnose()`의 SLA 규칙
  (지연 > 50 ms, 손실 > 1 %)뿐이다. IsolationForest는 Java 경로 `/anomaly`(`:279`)에서만 쓰인다.
- 그런데 README 43·53·251행, `experiments/experiment_report.md` §2.1·§2.2,
  `stress_test.py:156`("IsolationForest + adjacency scoring")이 폐쇄 루프가 IF 기반이라고 서술한다.
  README "핵심 기여 3 — 비지도 IsolationForest를 조합해 알려지지 않은 이상 패턴까지 커버"는
  폐쇄 루프에 관한 한 사실이 아니다.

#### P4. SecurityAnomalyDetector — 평가 누수 + 학습셋 오염

- `experiments/validate_security_detector.py:82-83`, `api_server.py:569-577` —
  `update()` **후** `detect()` 순서라 판정 대상 샘플이 이미 학습셋(최근 200개)에 포함된 채 판정한다.
- `anomaly_detector.py:129` — 레이블과 무관하게 모든 샘플로 재학습한다. 공격이 지속되면
  IsolationForest가 공격을 '정상'으로 학습한다.
- `contamination=0.05` vs CICDDoS2019 공격 비율 0.56 — 모델 가정이 데이터와 맞지 않는다.
- **의미**: README는 F1 < 베이스라인의 원인을 피처 추출에만 두고 있으나 이 구조적 문제도 함께
  작용한다. (contamination 조정은 "지표 튜닝"이므로 하지 않고 문서화만 한다.)

#### P5. MAML 학습에 탐색(exploration)이 없음

- **근거**: `agents/few_shot_agent.py:75` — 학습용 `_collect_episode()`가 항상 `argmax`로
  행동을 고른다. REINFORCE는 정책에서 **샘플링**해야 gradient 추정이 유효하다. 결정론적 선택이면
  같은 상태에서 늘 같은 행동만 보므로 advantage가 행동의 좋고 나쁨이 아니라 상태 차이를 반영한다.
- **의미**: `maml_only` 성공률 24%의 유력한 원인. 현재의 "MAML 기여가 확인되지 않음" 결론이
  MAML 자체의 한계인지 구현 결함인지 구분할 수 없다.

#### P6. Support buffer 라벨 오염

- **근거**: `api_server.py:372` — 이상이 없는 스텝에서는 `_prev_action`이 갱신되지 않아 다음 스텝에
  `(obs, 실행하지 않은 stale action, reward)`가 버퍼에 들어간다. 첫 이상 직전 샘플은
  항상 `action=0`(`r1-r2@10`)으로 기록된다(`:101` 초기값).
- **의미**: MAML inner-loop가 잘못된 (행동 → 보상) 쌍으로 적응한다.

#### P7. NO-OP 행동이 없음

- 행동 30개가 전부 cost 변경이다. 오프라인 평가(`run_experiment.py`)와 PPO 학습에서 에이전트는
  정상 상태에서도 매 스텝 어떤 링크의 cost를 반드시 바꿔야 한다.
- Java 경로 `OrchestrationService.onMetric()`도 `isAnomaly` 결과를 로그만 남기고
  이상 여부와 무관하게 5초마다 행동을 실행한다.
- **제약**: `FIX_PLAN.md` §0.2-5가 "NetworkEnv 30 행동"을 공개 시그니처로 못 박았으므로
  행동 공간은 바꾸지 않는다. Java 경로의 "이상 없으면 무행동"만 고치고 나머지는 한계로 기록한다.

#### P8. (수정 중 발견) 학습된 정책이 전부 상태 무관 상수 행동이다

- **근거**: 링크별 혼잡을 주입하고 8스텝씩 관측한 결과 (검사 코드는 2026-09-09 이후
  `ai-engine/policy_check.py`로 승격 — `python ai-engine/policy_check.py`로 재현):

  | 체크포인트 | 실제 관측에서의 행동 | 무작위 관측 200회 |
  | --- | --- | --- |
  | PPO (2026-09-06, 50k steps) | `r3-r4@100` 48/48 | `r2-r4@200` 113, `r3-r4@100` 38, … |
  | MAML 재학습 (2026-09-09, 샘플링 롤아웃) | `r3-r4@100` 48/48 | `r3-r4@100` 199/200 |
  | MAML 개정 전 (argmax 롤아웃) | `r1-r2@200` 42/48, `r2-r3@10` 6/48 | `r1-r2@200` 198/200 |

- **의미**: 오프라인 평가의 "PPO 성공 50% vs MAML 50%"는 두 상수 행동의 비교다 — TEST 링크 두 개 중
  `r3-r4`에서만 성공하는 이유는 상수 행동이 우연히 `r3-r4@100`이기 때문이다. 절제 실험의 `maml_only`
  성공 14%(=`r3-r4` 에피소드 7/50)도 같은 이유다. **어느 에이전트도 상태에 따라 링크를 고르는 정책을
  학습하지 못했다.** 원인 후보: 학습 중 이상 주입 확률 0.03으로 "행동이 보상을 바꾸는" 스텝이 드물고,
  보상이 평균 지연·대역폭 위주라 30개 행동 간 차이가 작다. 보상·커리큘럼 재설계 없이는 해결되지
  않으므로 이번 작업에서는 수정하지 않고 기록한다 (§2.3 후속 과제).

#### O7. (수정 중 발견) `requirements.txt` 고정 버전으로는 커밋된 체크포인트를 읽을 수 없다

- `ppo_network.zip` 내부 `system_info.txt`: Python 3.14.2 / SB3 2.8.0 / PyTorch 2.11 / **numpy 2.4.0**.
  `requirements.txt`는 torch 2.3.1 / numpy 1.26.4 / SB3 2.3.2를 고정하고 있었고, 그 환경에서는
  `ModuleNotFoundError: numpy._core` 로 PPO 로드가 실패하며 **`api_server.py`가 import 단계에서
  죽어 AI 엔진 전체가 뜨지 않았다** (베이스라인이 폐쇄 루프에 필요 없는데도). → A1에서 함께 수정.

### 1.2 코드 결함

| # | 위치 | 문제 |
| --- | --- | --- |
| C1 | `api_server.py:219-238` | `_fetch_snmp()` 실패 시 **"정상" 기본값**(bw 500, lat 10, loss 0)을 반환 → 장비 다운·네트워크 단절이 "SLA 정상, 조치 불필요"로 보고된다. `_apply_ospf()` 실패도 삼키고 응답은 `applied: True`. 자율 관리 시스템에서 가장 위험한 실패 모드. `environment/network_env.py:186-190` HTTP 모드도 동일 |
| C2 | `orchestrator/.../AiEngineClient.java:57` | `isAnomaly()` 예외 시 `false` 반환 — AI 엔진 다운이 "이상 없음"으로 조용히 처리됨 (C1과 같은 패턴) |
| C3 | `api_server.py:99-103` | 전역 가변 상태(`_support_buffer`, `_prev_obs`, detector 버퍼)를 동기 엔드포인트가 락 없이 공유. FastAPI는 동기 핸들러를 스레드풀에서 돌리므로 동시 `/auto-step` 호출 시 경합. Flask(`threaded=True`) 쪽 `metric_generator` 전역 dict도 무보호 |
| C4 | `anomaly_detector.py:27,129` | 최소 샘플 이후 **매 샘플마다** IsolationForest 재학습 (`/auto-step` 1회당 4번 `fit`) |
| C5 | `experiments/run_experiment.py:240-246` | `--sample-efficiency`가 `train_fewshot()`을 그대로 호출해 운영 체크포인트 `maml_network.pt`를 **덮어쓴 뒤** 임시 경로로 복사. 실험 한 번이면 운영 모델이 마지막 루프(200 iter)의 것으로 바뀐다 |
| C6 | `*/pom.xml`, `*/application.yml` | `spring-boot-starter-data-jpa` + PostgreSQL datasource 설정으로 **PostgreSQL 없이는 Java 서비스가 기동하지 않는다**. 영속화 코드는 0줄. `ddl-auto: update`는 운영 부적합 |
| C7 | `anomaly_detector.py:112` | `unique_src_count` 임계치 주석 "5초 내 500 IP" vs 실데이터 로더 1초 윈도우 — 단위 불일치 |
| C8 | 4곳 | 토폴로지 상수(`NODES`/`LINKS`/인접 링크)가 `metric_generator`, `anomaly_detector`, `network_env`, `AiEngineClient`에 각각 하드코딩 |

### 1.3 프로젝트 운영

| # | 문제 |
| --- | --- |
| O1 | 테스트·CI 전무 — `__main__` 자가 테스트 2개(`ospf_security.py`, `cicddos_loader.py`)뿐. C5 같은 회귀를 잡을 수단이 없다 |
| O2 | 재현 불가 — `stress_test.py`/`persistent_buffer_test.py`에 seed 없음, 시뮬레이터 노이즈 seed 고정 수단 없음, 학습 seed 없음 |
| O3 | 실험 임시 체크포인트(`maml_eff_tmp.pt`, `ppo_eff_tmp.zip`, `ppo_scratch.zip`)가 커밋됨 |
| O4 | 문서 자기모순 — `PROJECT_OVERVIEW.md` §10 "다음에 손댈 것" 2~4번이 같은 문서 §7에서 "해결"로 표시된 항목. README 핵심 기여 5 "완전한 일반화"는 규칙 기반 Analytics의 성질이지 학습 일반화가 아님 |
| O5 | 루트 `index.html` == `dashboard.html` 바이트 동일 중복 (GitHub Pages 요구 — 유지하되 명시) |
| O6 | 보안: `/debug/*` 무인증, CORS `*`, `0.0.0.0` 바인딩, OSPF 키 하드코딩 — 연구 데모로 허용 가능하나 README에 명시 없음 |

---

## 2. 수정 계획

### 2.1 태스크 목록

| ID | 우선순위 | 대상 문제 | 코드 변경 | 결과 재측정 |
| --- | --- | --- | --- | --- |
| **A1** | P0 | P1 시뮬레이터 시간 분리 | `metric_generator`, `mock_snmp_agent`, `api_server`, `network_env`, 실험 3종 | 필요 |
| **A2** | P0 | P2 RCA 오판 + 부수 피해 지표 | `anomaly_detector`, `api_server`, 실험 3종 | 필요 |
| **A3** | P0 | P5, P6 MAML 탐색·버퍼 라벨 | `few_shot_agent`, `api_server` | MAML 재학습 필요 |
| **A4** | P1 | C1, C2, C3, P7(Java) 조용한 실패·경합 | `api_server`, `network_env`, Java 2개 | 불필요 |
| **A5** | P1 | P4 탐지기 평가 누수 | `api_server`, `validate_security_detector` | CSV 없어 불가 → 문서화 |
| **A6** | P2 | C4–C8, O3 | 여러 파일 | 불필요 |
| **A7** | P3 | P3, O2, O4 실험 재실행 + 문서 동기화 | 실험 결과, README, OVERVIEW, report | — |

의존관계: A1 → A2 → A7, A3 → A7. A4/A5/A6 독립. **A7은 마지막.**

### 2.2 태스크 상세

#### A1. 시뮬레이터 시간을 관측에서 분리

- `metric_generator.tick()` 신설. `get_all_metrics()`/`get_node_metrics()`/`get_security_metrics()`는
  **순수 조회**(스트레스 갱신 없음). `random.Random` 인스턴스를 두고 `reset_state(seed=)`로 재현 가능.
- `mock_snmp_agent`: `POST /debug/tick` 추가, `POST /debug/reset` body `{"seed": N}` 지원.
  `SIM_CLOCK` 환경변수 — `lockstep`(기본, tick 요청으로만 진행) / `realtime:<ms>`(백그라운드 스레드가
  주기적으로 tick — Java·대시보드 데모용).
- `api_server /auto-step`: 사이클 시작 시 `POST /debug/tick` 1회 → 관측. 사이클당 정확히 1틱.
  기존 시맨틱(관측이 곧 1틱)과 동일하되, 외부 관측이 시간을 진행시키지 않는다.
- `NetworkEnv.step()`: cost 적용 → `tick` → 관측. `reset()`은 tick 없이 관측.
- 실험 스크립트: 검증용 `/metrics`는 이제 순수 조회. `time.sleep`은 무의미하므로 제거.
- 검증: `metric_generator.py` `__main__` 자가 테스트(조회 100회 후 스트레스 불변, tick 후 변화, seed 재현).
- 완료 기준: 위 자가 테스트 통과, `/auto-step` 응답 스키마 불변, 재현 스크립트에서 1틱/사이클 확인.

#### A2. RCA 2사이클 이후 오판 수정 + 부수 피해 지표

- `_root_cause_analysis()`를 `anomaly_detector.root_cause_analysis()`로 이동(api_server는 import).
  규칙:
  1. 위반 노드 **전부**에 인접한 `unhandled` 링크가 있으면 → 그것(가장 낮은 cost). 확정 근본 원인.
  2. 없고, 위반 노드 전부에 인접한 **이미 대응된**(cost ≥ 100) 링크가 있으면 → `None`
     ("이미 조치됨, 회복 대기"). 정상 링크를 건드리지 않는다.
  3. 둘 다 없으면 기존 폴백 `(-shared, cost)` 최소.
- `/auto-step` analytics-only(`disable_maml`) 경로: `root_cause`가 `None`이면 **무행동**
  (`act.applied=False, reason="recovering"`). 기존의 `unhandled[0]` 폴백 삭제.
- 실험 3종에 지표 추가: `wasted_actions`(주입 링크가 아닌 링크의 cost 변경 횟수),
  `rca_all_cycles_ok`(모든 사이클에서 root cause ∈ {주입 링크, None}). 기존 `root_match`는 유지.
- 검증: `anomaly_detector.py` `__main__`에 3규칙 케이스 + 2사이클 시나리오 자가 테스트.
- 완료 기준: 재현 스크립트에서 analytics-only `wasted_actions`가 0, 자가 테스트 통과.
  TTR 변화(전/후)를 §3에 기록 — **나빠져도 그대로 기록**.

#### A3. MAML 학습 탐색 + support buffer 라벨

- `_collect_episode(..., sample=True)`: 학습 시 `Categorical(logits).sample()`, 추론(`act`)은 argmax 유지.
- `api_server`: `_prev_action: int | None`. 행동을 실행하지 않은 스텝은 `None` → 다음 스텝에서 버퍼에
  넣지 않는다. `/reset-buffer`도 `None`으로.
- torch 설치 가능 시 MAML 재학습(500 iter, 기존 하이퍼파라미터 그대로 — **튜닝 금지**) 후
  오프라인 평가·ablation `maml_only`를 재측정. 불가 시 "체크포인트는 수정 전 학습분"을 명시.
- 완료 기준: 학습 코드가 샘플링 사용, 버퍼에 stale action 없음(자가 테스트), 전/후 수치 §3 기록.

#### A4. 조용한 실패 제거 + 루프 락

- `_fetch_snmp()` 실패 → `HTTPException(503, "SNMP agent unreachable")`. `/diagnose` 동일.
- `_apply_ospf()` → `bool` 반환, `act.applied`에 반영, 실패 시 `act.error` 포함.
- `NetworkEnv` HTTP 모드: 관측 실패 시 예외(가짜 정상값 삭제).
- Java `AiEngineClient.isAnomaly()`: 예외를 `RuntimeException`으로 전파, `OrchestrationService`가
  `log.error` 후 해당 라운드의 이상 판정을 "unknown"으로 처리. `anomaly-check-enabled=true`일 때
  **이상 노드가 하나도 없으면 orchestration 스킵**(폐쇄 루프 B와 동일 정책).
- `api_server`: `threading.Lock`으로 `/auto-step`·`/reset-buffer`·`/action` 상태 접근 직렬화.
  `metric_generator`: 모듈 락으로 tick/set/inject 직렬화.
- 검증: `mvn -q compile` 양쪽, FastAPI TestClient로 SNMP 다운 시 503 확인.

#### A5. SecurityAnomalyDetector 평가 순서

- `/security/detect`, `validate_security_detector.py`: **`detect()` → `update()`** 순서로 변경.
  `update()/detect()` 시그니처(6인자) 불변.
- `contamination` vs base-rate 불일치와 "공격 지속 시 공격을 정상으로 학습" 문제를 README 한계에 추가.
  CSV(1.87 GB)가 이 환경에 없어 재실행 불가 → `cicddos_validation_v2.json`에 `_note`로
  "update→detect 순서로 측정된 값, 다음 재실행은 detect→update" 명시.

#### A6. 코드 결함 모음

- C4: `AnomalyDetector`/`SecurityAnomalyDetector` 재학습을 `_refit_every`(기본 10샘플) 주기로.
- C5: `train_fewshot(save_path=)` 인자 추가, 샘플 효율 실험은 임시 경로에 직접 저장.
- C6: 두 `pom.xml`에서 `data-jpa`/`postgresql` 제거, `application.yml`에서 datasource/jpa 삭제.
  compose의 PostgreSQL/Redis 주석 갱신.
- C7: 주석을 "윈도우당 500 IP (시뮬레이션 5초, CICDDoS 로더 1초 — 윈도우 길이에 따라 의미가 다름)"으로.
- C8: `ai-engine/topology.py` 신설(NODES/LINKS/NODE_LINKS/OSPF_COSTS), `anomaly_detector`·`network_env`·
  `api_server`가 import. `simulation/metric_generator`는 별도 배포 단위라 자체 상수를 유지하되
  자가 테스트에서 동일성 검증.
- O3: 임시 체크포인트 3개 `git rm`, `.gitignore`에 `*_tmp.pt`, `*_tmp.zip`, `ppo_scratch.zip`.

#### A7. 실험 재실행 + 문서 동기화

- `mock_snmp_agent` + `api_server` 기동 → `ablation_study.py`, `stress_test.py`,
  `persistent_buffer_test.py` 재실행. 결과 파일은 **스크립트 출력으로만** 갱신, 이전 결과는
  `*_pre_audit.json`으로 보존.
- README/`experiment_report.md`/`PROJECT_OVERVIEW.md`: IsolationForest 서술 정정(P3), 새 수치를
  이전 수치와 **병기**, §10 모순 정리(O4), 보안 데모 범위 명시(O6), 이 문서 링크 추가.

---

### 2.3 이번에 하지 않은 것 (후속 과제)

- **P7 NO-OP 행동** — 행동 공간(30)은 공개 시그니처. Java 경로의 "이상 없으면 무행동"만 적용.
- **P8 정책 붕괴** — 보상·커리큘럼 재설계가 필요. 임계치/하이퍼파라미터 조정으로 좋게 보이게 만들지 않는다.
- **P4 contamination vs base rate** — 튜닝하지 않고 문서화. CICDDoS2019 CSV가 이 환경에 없어 재실행 불가.
- **C8 Java 쪽 토폴로지 상수** — `AiEngineClient.NODE_ORDER/LINK_ORDER`는 그대로 (별도 언어).
- **O5 `index.html` 중복** — GitHub Pages 요구. 문구만 동기화.

---

## 3. 수행 결과 (2026-09-09)

커밋: `ec441e9`(감사) → `bb7873b`(A1) → `957d0f5`(A2) → `1ea0295`(A6) → `39b6df0`(A3+A4) →
`5b1e85d`(A5) → `2591e4b`(문서) → `8f41034`(MAML 재학습) → 결과 파일 커밋들.
검증 환경: Linux, Python 3.11, numpy 2.4.6, torch 2.14, SB3 2.8.0, Java 17 + Maven.
**이전 결과 파일은 `experiments/results/*_pre_audit.json`으로 보존**했고, 새 결과는 전부 스크립트
출력이다 (수기 편집 없음).

### 3.1 측정 조건의 변화

| | 개정 전 | 개정 후 |
| --- | --- | --- |
| OODA 1사이클당 시뮬레이터 틱 | **2** (검증 조회가 1틱 추가) | **1** |
| 오프라인 평가 1스텝당 틱 | **2** (로깅용 재조회) | **1** |
| 노이즈 seed | 없음 | 에피소드별 고정 |
| RCA | 2사이클째부터 오판 | 3규칙 (§2.2 A2) |
| 지지 버퍼 | 실행 안 한 행동 기록 | 실행한 행동만 |
| MAML 학습 롤아웃 | argmax | 정책 샘플링 |

따라서 **아래 전/후 수치는 같은 자로 잰 것이 아니다.** "TTR이 나빠졌다"가 아니라 "이전 수치가 실제
사이클 수의 약 절반이었다"로 읽어야 한다. 시뮬레이터의 물리 하한: cost 100을 즉시 적용해도 SLA
회복까지 4틱 (`metric_generator.py` 자가 테스트), 폐쇄 루프는 1틱 뒤 첫 관측 → 행동이므로 6~7사이클이
사실상 최선이다.

### 3.2 폐쇄 루프 — 스트레스 테스트 (`stress_50ep.json`, 50 ep, seed 42)

| 지표 | 개정 전 (2틱/사이클) | **개정 후 (1틱/사이클)** |
| --- | --- | --- |
| Avg TTR | 3.78 | **6.84** (TEST 6.69 / TRAIN 7.11) |
| 성공률 | 100% | **100%** |
| RCA 정확도 (첫 사이클) | 100% | **100%** |
| RCA 전 사이클 정확도 | 측정 안 함 | **100%** |
| 부수 피해 (정상 링크 cost 변경/ep) | 측정 안 함 | **0.80** |
| TTR 분포 | 2:4% 3:30% 4:50% 5:16% | 4:2% 5:2% 6:42% 7:32% 8:12% 9:6% 10:4% |

부수 피해 0.80은 전부 MAML의 2번째 이후 행동(`r3-r4@100` 상수, P8)이다. Analytics는 정상 링크를
건드리지 않는다.

### 3.3 절제 실험 (`ablation_study.json`, 모드당 50 ep, 에피소드별 seed 고정)

| 모드 | 개정 전 TTR / 성공률 | **개정 후 TTR / 성공률** | 부수 피해/ep | RCA 전 사이클 |
| --- | --- | --- | --- | --- |
| analytics_only | 3.88 / 100% | **6.94 / 100%** | **0.00** | 100% |
| maml_only | 12.32 / 24% | **13.80 / 14%** | 0.86 | — |
| combined | 3.80 / 100% | **6.94 / 100%** | 0.86 | 100% |

결론은 개정 전과 같고 더 선명해졌다: **복구는 전부 Analytics가 한다.** combined는 analytics_only와
TTR이 같고 부수 피해만 0.86/ep 늘어난다 — 현재 MAML의 기여는 음(−)이다. maml_only 성공 14%는
상수 행동 `r3-r4@100`이 정답인 에피소드(7/50)뿐이다.

오프라인 재현 스크립트(`cowork/scratch/rca_trace.py`, HTTP 없음)의 analytics-only 결과도 일치한다:
개정 전 코드 3.94(2틱)/6.68(1틱)에 정상 링크 오변경 112/208회 → 개정 후 **6.86, 오변경 0회**.

### 3.4 지속 버퍼 (`persistent_buffer.json`, 30 ep)

| | 개정 전 | **개정 후** |
| --- | --- | --- |
| Avg TTR 전체 / 초기 / 후기 | 3.73 / 3.73 / 3.73 | **6.93 / 7.00 / 6.87** |
| 적응 스텝/ep 초기 → 후기 | 3.07 → 2.93 | 5.47 → 6.00 |
| RCA 정확도 | 86.7% | **100%** |
| 부수 피해/ep | — | 0.73 |

버퍼가 쌓여도 2번째 행동은 30/30 에피소드에서 `r3-r4@100`(상수)이다. 개정 전 문서의 "2차 행동이
`r2-r3@10`(no-op)으로 바뀌는 것을 학습했다"는 해석은 이번 결과로는 지지되지 않는다.

### 3.5 오프라인 평가 (`offline_eval_post_audit*.json`, TEST 링크, 50 ep, 1틱/스텝)

| 에이전트 | 개정 전 (2틱/스텝) | **개정 후 (1틱/스텝)** |
| --- | --- | --- |
| PPO (50k steps, 재학습 안 함) | 100.9 / 50% | **101.8 / 50%** |
| MAML 개정 전 체크포인트 (argmax 학습) | 139.7 / 32% | **188.6 / 6%** |
| MAML 재학습 (샘플링 롤아웃, 500 iter, seed 42) | — | **101.8 / 50%** |

샘플링으로 재학습한 MAML은 PPO와 **동일한 상수 정책**(`r3-r4@100`)으로 수렴했다 (P8). 개정 전
MAML은 `r1-r2@200` 상수였다. 성공 50%는 TEST 링크 두 개 중 하나가 우연히 상수 행동과 일치하기
때문이며 정책 품질의 증거가 아니다.

> **2026-09-09 추가 확인 (ROADMAP §6)**: 이 표의 MAML 재학습 체크포인트는 `train_links`를 넘기지 않는
> 경로로 학습돼 **TEST 링크(r3-r4, r1-r4)가 학습에 섞였을 가능성이 높다**. 링크 분리를 보장한 재학습
> (`SIM_VERSION = 3`, `results/offline_eval.json`)에서는 PPO 200.0 / 0%, MAML 101.81 / 50%로
> 갈렸다 — 이 표의 "둘 다 101.8로 동등"은 누수의 산물로 보아야 한다.

### 3.6 재검증 안 된 것

- CICDDoS2019 (A5): CSV 없음. 결과 파일에 `_note`만 추가. 다음 실행은 `detect→update` 순서.
- Java 경로(A): `mvn compile` 양쪽 통과, Kafka 실기동은 미검증 (Docker 없음).
- PPO는 재학습하지 않았다 (A1의 틱 변경은 학습 환경(`NetworkEnv.step` 1틱)에는 영향이 없었다 —
  오프라인 *평가* 루프의 로깅 재조회만 2틱이었다).
